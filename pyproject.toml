[project]
name = "zakey-frontend-reference-build"
version = "0.1.0"
description = "Frontend-only Django template reference build for ZAKEY v2"
requires-python = ">=3.12,<3.13"
dependencies = [
    "Django==5.2.16",
]

[tool.uv]
package = false
