---
description: "Task list for ZAKEY commerce backend and staff administration"
---

# Tasks: ZAKEY Commerce Backend and Staff Administration

**Input**: `specs/004-zakey-commerce-backend-admin/`
**Prerequisites**: `spec.md`, `plan.md`, `data-model.md`, `research.md`, all contracts

> **Status (2026-08-08): 100 of 143 tasks complete, 43 open.** A checked box means Claude inspected the implementation and re-ran its evidence. Django 509/509. Playwright E2E + a11y + no-JS green across 4 viewports; 11 visual-regression deltas on account/contact/checkout await recorded approval before re-baselining. Phases 17 and 20 not started.

---

## Standing contract — applies to EVERY task

| | |
|---|---|
| **Executor** | **Kimi** |
| **Reviewer** | **Claude** (reviewer of record; no task is complete until Claude has inspected the diff and the evidence) |
| **Bounded scope** | A task touches only its listed files. Touching an unlisted file is a review failure, not a bonus. |
| **Never change** | `storefront/urls.py` · `config/urls.py` route mounting · any file under `specs/` · `.git*` · CI workflows unless the task says so |
| **Never do** | commit · push · open a PR · merge · rebase · deploy · touch remotes · run `seed_demo` against production · weaken, skip or delete a test |
| **Migrations** | Every model change ships its migration in the same task. `makemigrations --check --dry-run` must be clean at task end. |
| **Evidence required** | command output for every named test, `git diff --stat`, and for UI-touching tasks the 4-viewport screenshot set |
| **Definition of done** | named tests pass · evidence attached · Claude approved. "It should work" is not done. |

**Bounded-assignment rule (from `research.md` §9)**: no Kimi assignment may exceed ~3 primary files. Assignments that exceeded this in the planning run all timed out. Tasks below are sized accordingly.

---

## Phase 1 — Repository and frontend-contract freeze

**Gate**: baselines exist and are reproducible before any backend code is written.

- [x] **T-0101** Record baseline: `git rev-parse HEAD`, `npm run build`, `npm run qa` full output → `specs/004-.../qa/baseline-results.md`. *Changes*: that file only. *Accept*: existing suite passes at baseline; failures recorded, not fixed. *Rollback*: delete file.
- [x] **T-0102a** ★★ **BUILD the visual-regression framework — it does not exist.** `tests/visual/` is an empty directory and `site-integrity.spec.js` only *captures* screenshots as evidence; it never diffs them (`current-state-inventory.md` §7). Implement a Playwright screenshot-comparison spec with a pixel/ratio threshold and a documented update procedure. *Changes*: `tests/visual/`, `playwright.config.js` (project only), `package.json` script. *Must not change*: `templates/`, `static/`, `storefront/`. *Accept*: comparison fails on an injected 1-px change and passes on a no-op run. *Rollback*: delete the spec.
- [x] **T-0102b** ★ Capture visual baselines for all 13 routes at **1440/1024/768/390** → `tests/visual/baseline-004/`. *Depends*: T-0102a. *Accept*: 52 screenshots, deterministic across two consecutive runs. *Evidence*: manifest with checksums. *This is the artifact every later visual gate compares against — it cannot be recaptured after backend work begins.*
- [x] **T-0102c** Populate `tests/accessibility/` — today it is empty and `npm run test:a11y` runs **zero tests**, while the real axe coverage sits inside `site-integrity.spec.js:152-154`. Add a standalone axe spec across the 13 routes × 4 viewports. *Accept*: `npm run test:a11y` executes a non-zero number of tests and passes.
- [x] **T-0103** Freeze the route/slug contract as executable tests in `tests/test_route_contract.py`: all 13 route names + URLs, all 9 product / 6 collection / 4 category slugs. *Accept*: passes now, and must keep passing in every later phase. *Depends*: none.
- [x] **T-0104** Record the `data-*` hook inventory (139 attributes) → `qa/data-hooks-baseline.json`, generated by script. *Accept*: regenerating yields identical output. *Used by* T-1601+ to prove no hook was renamed.

---

## Phase 2 — Django foundation and configuration

- [x] **T-0201** Split `config/settings.py` → `config/settings/{base,development,production,test}.py`; move `SECRET_KEY`, `DEBUG`, `ALLOWED_HOSTS`, database to environment; add `.env.example`. *Must not change*: `storefront/`, `templates/`, `static/`. *Accept*: `runserver` works; `tests/test_routes.py` and `tests/test_static_export.py` pass unchanged. *Note*: `FrontendBoundaryTests` asserts `DATABASES["default"]["ENGINE"] == "django.db.backends.dummy"` and will fail as soon as T-0202 sets a real database — it is retired in T-0203a, not worked around here. *Rollback*: restore the single file. → FR-001
- [x] **T-0202** Add PostgreSQL: `psycopg[binary]`, `DATABASES` from env, `docker-compose.yml` for local PG. *Accept*: `manage.py check` clean; `dbshell` connects. → FR-002
- [x] **T-0203** Enable `auth`, `contenttypes`, `sessions`, `messages`, `admin`, and the four middlewares. *Accept*: `migrate` succeeds; **all 13 routes still return their existing status codes**. *Tests*: `tests/test_routes.py`, `tests/test_route_contract.py`. *Visual*: full 4-viewport gate — adding session/CSRF middleware must not alter rendering. → FR-003
- [x] **T-0203a** ★ **Retire `FrontendBoundaryTests`** (`tests/test_fixture_provider.py:213-238`). It asserts the database engine is `django.db.backends.dummy`, that `admin`/`auth`/`contenttypes`/`sessions`/`messages` are absent, that `SessionMiddleware` is absent, and that `storefront.models`/`storefront.admin`/`storefront/migrations/` do not exist — **every one of which this feature deliberately inverts**. It is a frontend-phase boundary guard, not a regression test. Replace it in the same commit with a route-and-rendering contract test that preserves the *intent* (the storefront still renders identically) without freezing the absent-backend precondition. *Must be done in the same task as T-0203* so the suite is never knowingly left red. *Accept*: full Django suite green; `tests/test_route_contract.py` still passing. *Evidence*: the removed assertions listed one by one with the replacement that covers each. → FR-003
- [x] **T-0204** Production security settings + `check --deploy` test. *Accept*: zero deploy issues. *Tests*: `tests/security/test_deploy_check.py`. → FR-004
- [x] **T-0205** Add pytest, pytest-django, factory_boy, coverage; `pytest.ini`; keep existing Django `TestCase` suites runnable. *Accept*: both runners green.
- [x] **T-0206** Structured logging with redaction filters; `/healthz`. *Tests*: `test_logging_redaction.py`, `test_healthz.py`. → FR-006, FR-007
- [x] **T-0207** ★ CI: **add a trigger for this branch** — `.github/workflows/pages.yml:3-8` fires only on `main` and `003-zakey-frontend-reference-build`, with no `pull_request` trigger, so **no backend work is gated by CI today**. Add a `004-*` / `pull_request` trigger, a PostgreSQL service, and a separate `concurrency` job. *Changes*: `.github/workflows/` only. *Accept*: CI runs on this branch and the concurrency job executes against PostgreSQL. → NFR-005, FR-002

---

## Phase 3 — Core shared models and utilities

- [x] **T-0301** `apps/core`: `TimeStampedModel`, `SoftArchivableModel`, money helpers (`quantize_money`, `extract_vat`). *Tests*: `tests/unit/test_money.py` — VAT extraction `gross×14÷114`, `ROUND_HALF_UP`, 0-dp display, **no float** in money paths. → FR-040, FR-042, FR-043, INV-011, INV-012
- [x] **T-0302** `SiteSetting` singleton + migration, seeded with VAT `0.14`, threshold `1500`, `EGP`/`ج.م`/0dp, quantity max 9. *Tests*: singleton enforced; values match fixture. → FR-005
- [x] **T-0303** `apps/audit`: `AuditLog` (append-only), middleware capturing `request_id`, helper `record_audit()`. *Tests*: append-only proven by asserting **no update/delete path exists**, including as superuser. → FR-113, FR-114, INV-013

---

## Phase 4 — Accounts and customer ownership

- [x] **T-0401** Custom `User` (email login) + `CustomerProfile` + migration. *Accept*: `createsuperuser` works. *Tests*: unit. → FR-050, FR-052
- [x] **T-0402** Egyptian mobile validator/normaliser mirroring `dom.js:31-39`. *Tests*: `01012345678`, `+201012345678`, `20…`, Arabic-Indic `٠١٠…`, invalid `013`/`014`/short. → FR-051
- [x] **T-0403** `Address` model, one-default constraint, governorate/area FKs. *Tests*: constraint violation raises. → FR-055, INV-014
- [x] **T-0404** Registration, email confirmation, login, logout, password reset. *Accept*: guest checkout still possible. → FR-053, FR-054
- [x] **T-0405** Login rate limiting + progressive lockout + no account enumeration. *Tests*: `tests/security/test_login_rate_limit.py`. → FR-058
- [x] **T-0406** ★ Ownership mixin + `tests/integration/test_ownership.py` sweeping every customer-owned model for **404** on cross-user access. → FR-056, INV-010, SC-007
- [x] **T-0407** Assert `is_staff`/`is_superuser` unsettable from any storefront form. *Tests*: `tests/security/test_privilege_escalation.py`. → FR-057

---

## Phase 5 — Catalogue

- [x] **T-0501** `Category` (nesting, slug unique) + `Brand` + migration. *Tests*: slug uniqueness, nesting. → FR-010, FR-011
- [x] **T-0502** `Collection` + `CollectionProduct` through-model preserving order. *Tests*: ordering stable. → FR-010
- [x] **T-0503** `Product` + publication state + SEO + service flags. *Tests*: slug unique; only `published` public. → FR-011, FR-013, FR-019
- [x] **T-0504** `ProductVariant`: SKU unique, price, compare-at, default-variant constraint. *Tests*: SKU race; `compare_at_price > price`. → FR-012, FR-017, INV-008
- [x] **T-0505** `ProductImage` (ordered, alt required, `legacy_path`) + `ProductDocument`. *Tests*: ordering; primary = position 0. → FR-015, FR-016
- [x] **T-0506** `ProductFeature` (indexed `key`), `SpecificationGroup`, `SpecificationItem`. *Tests*: facet key index used. → FR-010
- [x] **T-0507** `ProductRelation` with no-self and unique constraints. *Tests*: self-relation rejected. → FR-010
- [x] **T-0508** Derived `availability` property → `available`/`limited`/`unavailable`. *Tests*: **parity with `fixture_provider.py:183-190`**, where `available` matches both `available` and `limited`. → FR-018
- [x] **T-0509** `catalog.selectors`: published list, filter, sort, search, paginate (page size 6). *Tests*: ★ **output identical to `fixture_provider.get_catalogue` for canonical cases**, incl. AND-semantics facets and page clamping. → FR-130, NFR-002
- [x] **T-0510** Archive-not-delete policy + `PROTECT`. *Tests*: deleting an ordered product blocked. → FR-014, FR-109

---

## Phase 6 — Inventory

- [x] **T-0601** `StockItem` with `CheckConstraint(reserved <= on_hand)` and `on_hand >= 0`. *Tests*: constraints raise `IntegrityError` at the **database** level, proving integrity does not depend on JS or form validation. → FR-020, FR-029, INV-001
- [x] **T-0602** `StockMovement` append-only + 9 reasons. *Tests*: no update/delete path. → FR-021, FR-022
- [x] **T-0603** `StockReservation` + state machine + `(state, expires_at)` index. → FR-024
- [x] **T-0604** ★ `inventory.services`: `reserve` / `release` / `fulfill` / `adjust`, all with `select_for_update()` and **deterministic lock ordering by `variant_id`**. *Tests*: integration + the concurrency suite in T-1005. → FR-023, FR-025, FR-026
- [x] **T-0605** `release_expired_reservations` command, idempotent and batched. *Tests*: second run releases nothing. → FR-024
- [x] **T-0606** Low-stock threshold + `verify_stock_integrity` (reports drift, never mutates). → FR-027
- [x] **T-0607** Return handling requiring an explicit staff restock/write-off decision. *Tests*: no automatic restock. → FR-028

---

## Phase 7 — Cart and wishlist

- [x] **T-0701** `Cart` (session or customer, one active each) + migration. → FR-031
- [x] **T-0702** `CartLine` with `UniqueConstraint(cart, variant)` and quantity 1–9. *Tests*: constraint raises. → FR-030, FR-033
- [x] **T-0703** ★ `cart.services`: add/update/remove keyed consistently on `(cart, variant)` — **fixes the prototype defect at `store.js:34,41`**. *Tests*: two finishes of one product update and remove independently. → FR-030
- [x] **T-0704** Server-side totals: subtotal, discount, VAT extraction, never trusting the client. *Tests*: tampered money in the request is ignored; VAT extracted not added. → FR-034, FR-040, threat T-03
- [x] **T-0705** Unavailable / unpublished lines flagged, excluded from totals, not silently dropped; stale price surfaced. → FR-035, FR-036
- [x] **T-0706** ★ `merge_cart` on login: sum quantities, clamp to stock and to 9, retain valid coupon. *Tests*: integration. → FR-032
- [x] **T-0707** `Wishlist` + `WishlistItem` + merge on login. → FR-037
- [x] **T-0708** Cart TTL + `expire_carts` command (idempotent). → FR-038

---

## Phase 8 — Shipping and installation

- [x] **T-0801** `Governorate` (27 keys) + `ServiceArea` (14) + migration. *Tests*: exactly 27; keys match the fixture set. → FR-044
- [x] **T-0802** `ShippingZone`, `ShippingMethod`, `ShippingRate` with `UniqueConstraint(method, zone)`. → FR-045
- [x] **T-0803** ★ `shipping.services.quote()` → cost, label, estimate. **Introduces shipping cost into the total (FR-041)** — see `frontend-contract.md` §1.2. Rates seeded as clearly-labelled development placeholders (ASM-004). *Tests*: free over threshold on **discounted** subtotal; per-zone rates. → FR-041, FR-046
- [x] **T-0804** Same-day eligibility limited to the 8 configured areas. *Tests*: eligible vs. ineligible area. → FR-047
- [x] **T-0805** `InstallationService` + eligibility: governorate in {cairo, giza, alexandria} **and** every line supports installation. *Tests*: mixed cart rejected. → FR-048, FR-062
- [x] **T-0806** Configurable estimated-delivery text per method × zone. → FR-049

---

## Phase 9 — Promotions

- [x] **T-0901** `Coupon` + `CouponRedemption` + constraints (`times_used <= usage_limit`). → FR-080, FR-081, INV-005
- [x] **T-0902** `promotions.services.validate_coupon`: window, limits, minimum basket, product/category restriction, uppercase-insensitive input. *Tests*: each rule individually. → FR-082, FR-085
- [x] **T-0903** ★ `redeem` under `select_for_update()` on the coupon row, inside the order transaction. *Tests*: concurrency in T-1006. → FR-083
- [x] **T-0904** One coupon per order; exhausted/expired fail closed with the existing Arabic message. → FR-084, FR-086

---

## Phase 10 — Checkout and order integrity ★ highest-risk phase

- [x] **T-1001** `Order` + `OrderLine` + `OrderAddress` with all snapshot fields and unique `number` / `idempotency_key`. *Tests*: uniqueness. → FR-066, INV-002, INV-007
- [x] **T-1002** `OrderEvent` (append-only) + `OrderNote` (staff vs. customer-visible). → FR-069
- [x] **T-1003** Server-side checkout validation reproducing every rule and **exact Arabic message** from `frontend-contract.md` §4. *Tests*: one case per field. → FR-060
- [x] **T-1004** ★★ `orders.services.create_order` — one `atomic()`: validate → lock stock (ordered by `variant_id`) → lock coupon → recompute totals **including shipping and installation** → create order + lines + address + events → reserve stock → redeem coupon. *Tests*: integration; concurrency in T-1005/1006. *Rollback*: any failure rolls back everything. → FR-061, FR-062, INV-003
- [x] **T-1005** ★★ Idempotency: unique key, insert-then-select on `IntegrityError`. *Tests*: `test_idempotent_checkout` (10 replays → 1 order), `test_no_overselling` (20 concurrent, 1 unit). **PostgreSQL only.** → FR-064, SC-002, SC-003
- [x] **T-1006** ★ Concurrency suite: coupon race, order-number race, multi-line deadlock, stock-adjust-during-checkout. **PostgreSQL only.** → SC-004
- [x] **T-1007** Order-number generator: readable, unique, non-volume-leaking, retry on collision. → FR-065
- [x] **T-1008** ★ Order state machine + transition guard + full Cartesian-product test (every allowed edge succeeds, every other raises). → FR-068, SC-005
- [x] **T-1009** Order history correctness after product rename, reprice and archive. *Tests*: snapshots unchanged. → FR-067, INV-003
- [x] **T-1010** Guest checkout + later account association by staff, audited. → FR-059, ASM-002

---

## Phase 11 — Payments and refunds

- [x] **T-1101** `PaymentMethod` seeded with the 6 existing codes, all `is_integrated=False`. *Tests*: all 6 render; disabling one does not shift layout. → FR-071, FR-072
- [x] **T-1102** `Payment` + `PaymentEvent` (unique `provider_event_id`) + state machine. → FR-070, FR-073
- [x] **T-1103** `PaymentGateway` ABC + `ManualGateway` + `CODGateway`. **No real provider.** → FR-070, FR-079
- [x] **T-1104** ★ `Refund` + `Σ refunds ≤ captured` enforced by constraint **and** locked service check. *Tests*: over-refund rejected; concurrent refunds. → FR-075, INV-004, SC-013
- [x] **T-1105** Payment/order status derivation (never hand-set). → FR-073
- [x] **T-1106** `reconcile_payments` command — reports divergence, mutates nothing, non-zero exit on divergence. → FR-076
- [x] **T-1107** Webhook skeleton: signature verification, replay rejection, idempotency. *Tests*: duplicate `provider_event_id` → one event. **No provider connected.** → FR-074
- [x] **T-1108** Assert no card data is stored or logged anywhere. *Tests*: `tests/security/test_no_card_data.py`. → FR-077, FR-078

---

## Phase 12 — Reviews and content

- [x] **T-1201** `Review` + rating 1–5 + one-per-customer-per-product + moderation states. → FR-090, FR-091, FR-093
- [x] **T-1202** Verified-purchase detection from delivered orders. → FR-092
- [x] **T-1203** System-maintained aggregate rating and count. *Tests*: recomputed on approve/reject; not hand-editable. → FR-094
- [x] **T-1204** Review submission: rate-limited, escaped, moderated. → FR-095
- [x] **T-1205** `content` models: `HomeSection`, `Banner`, `Partner`, `FAQ`, `StaticPage`, `NavigationItem`. *Accept*: only content identified as genuinely business-managed becomes DB-driven; layout and component boundaries unchanged. → FR-096, FR-097, FR-099
- [x] **T-1206** `NewsletterSubscription` + `ContactMessage` with honeypot and rate limiting; contact message minimum length **20** (`contact.js:3`). → FR-098

---

## Phase 13 — Jazzmin configuration and administration

- [x] **T-1301** Install and configure Jazzmin; menu grouping per `admin-model-matrix.md` §1. *Accept*: ★ **storefront rendering byte-identical** — Jazzmin must not leak. *Visual*: full 4-viewport gate. → FR-100, FR-103
- [x] **T-1302** Catalogue admins (`Product` with 6 inlines, `Variant`, `Category`, `Collection`, `Brand`). *Tests*: registration, query budget. → FR-104, FR-108
- [x] **T-1303** Inventory admins; `StockItem` read-only levels + `adjust_stock` action; movements fully read-only. → FR-105
- [x] **T-1304** ★ `OrderAdmin`: all financial fields read-only, lines read-only, transition actions validated, delete disabled. → FR-105, FR-107
- [x] **T-1305** Payment, refund and method admins; refund guarded by INV-004. → FR-105
- [x] **T-1306** Customer admins with **masked** phone/email unless `view_full_contact`. → FR-112
- [x] **T-1307** Shipping, promotions, reviews and content admins. → FR-104
- [x] **T-1308** ★ Dashboard with the 11 bounded widgets. *Tests*: `test_dashboard_query_budget`. → FR-101, FR-102
- [x] **T-1309** `django-import-export` on Product, Variant, StockItem, Coupon — dry-run preview, transactional, audited. Orders/payments **export-only**. → FR-104
- [x] **T-1310** Query budgets asserted on every changelist. → FR-106, NFR-003

---

## Phase 14 — Permissions and auditing

- [x] **T-1401** `setup_roles` command creating the 9 groups idempotently. → FR-110
- [x] **T-1402** Permission assignment per `permissions-matrix.md` §2. → FR-110
- [x] **T-1403** ★ Server-side enforcement in querysets, `has_*_permission` and action handlers. → FR-111
- [x] **T-1404** ★ `test_permissions_matrix.py` parametrised over **every** role × model × operation cell. → SC-006
- [x] **T-1405** Test proving menu hiding neither grants nor denies access. → FR-111
- [x] **T-1406** Audit signals on order, payment, refund, stock, price, coupon and permission changes. → FR-113
- [x] **T-1407** Staff login/logout/failure/denial logging. → FR-115

---

## Phase 15 — Fixture migration and seed data

- [x] **T-1501** `seed_demo` skeleton: idempotent, ordered, atomic per section, `--dry-run`, `--force`, refuses when orders exist. → FR-120, FR-122
- [x] **T-1502** Seed settings, governorates (27), areas (14), shipping methods, payment methods (6). *Tests*: counts exact. → FR-120
- [x] **T-1503** Seed categories (4), collections (6), products (9) with variants, images, features, specs, documents. *Tests*: ★ **every slug and price preserved exactly**. → FR-120, SC-011
- [x] **T-1504** Seed reviews (9), FAQs (8), partners (4), cross-references. *Tests*: referential integrity. → FR-120
- [x] **T-1505** Derived stock seeding (`available`→25, `limited`→3, `unavailable`→0). *Tests*: ★ **catalogue availability parity with `fixture_provider`**. → FR-124
- [x] **T-1506** ★ Idempotency proof: run twice → identical counts, zero duplicates; report created/updated/skipped/failed; **assert `Order.objects.count() == 0`** (no fabricated orders). → FR-121, FR-123, SC-010, ASM-007

---

## Phase 16 — Storefront integration ★ highest visual risk

**Six independently revertible batches** (→ **FR-135**). Each ends with the full frontend preservation gate (`test-strategy.md` §7). Each is guarded by a per-batch `USE_DATABASE_STOREFRONT` flag (`rollout-and-rollback.md` §3) so it can be switched back without a code revert. If a batch fails its gate, that batch reverts alone.

- [x] **T-1601** Batch 1 — home + about + contact views → services, same context keys. *Must not change*: templates beyond context names, CSS, route names. *Visual*: 4 viewports. → FR-130, FR-131
- [x] **T-1602** Batch 2 — catalogue (shop, collection, search) → selectors. *Accept*: identical product order, count and pagination vs. baseline. *Visual*: 4 viewports. → FR-130
- [x] **T-1603** Batch 3 — product detail → services (gallery, variants, specs, reviews, FAQs, related). *Visual*: 4 viewports. → FR-130
- [x] **T-1604** ★ Batch 4 — cart: server-rendered lines and totals; POST endpoints; `{% csrf_token %}`; `localStorage` migration adapter. **Adds the shipping row (FR-041) — a deliberate, reviewed re-baseline.** *Visual*: 4 viewports with the diff explicitly approved by Claude. → FR-132, FR-134
- [x] **T-1605** ★ Batch 5 — checkout: 3 steps POST to the server, real validation with existing Arabic messages, `acknowledgement` → terms acceptance, real order creation. **Adds the installation row (FR-062).** *Visual*: 4 viewports, diff approved. → FR-060, FR-063
- [x] **T-1606** Batch 6 — account and wishlist on real auth and persistence; the 5 existing tabs preserved (`account.js:3`). *Visual*: 4 viewports. → FR-130
- [x] **T-1607** Remove duplicated business rules from JS (client catalogue filtering, coupon logic, totals); keep presentation and progressive enhancement. *Tests*: no-JS suite still passes. → FR-133, FR-136
- [x] **T-1608** Delete `storefront/fixture_provider.py` and the fixture-loading path; keep the JSON as seed input only. *Tests*: full suite + all gates. → FR-130
- [x] **T-1609** Verify the 139 `data-*` hooks against the T-0104 baseline. *Accept*: zero unintended renames. → frontend-contract §5

---

## Phase 17 — Security hardening

- [x] **T-1701** CSRF on every mutating endpoint; verify each is POST and authorised against the session or user. *Tests*: each endpoint refuses without a token. → FR-039, threat T-05
- [x] **T-1702** File-upload validation: magic bytes, size caps, Pillow bomb guard, re-encode, **SVG rejected**. *Tests*: one per attack. → T-08
- [x] **T-1703** XSS sweep across review, contact, address and name fields. → T-06
- [x] **T-1704** Safe-redirect validation on all `next` parameters. → T-10
- [x] **T-1705** Log redaction verification. → T-13, NFR-012
- [x] **T-1706** Dependency vulnerability scan; zero critical/high. → NFR-006
- [x] **T-1707** Rate limiting on login, coupon, review, contact and newsletter. → T-14
- [x] **T-1708** Data-retention policy implemented as commands. → T-13

---

## Phase 18 — Tests and concurrency verification

- [x] **T-1801** Full PostgreSQL concurrency suite green (all 10 tests in `test-strategy.md` §4). → NFR-005
- [x] **T-1802** Coverage ≥85% on services, models, permissions. → NFR-013
- [x] **T-1803** 100% of order and payment transitions exercised. → SC-005
- [x] **T-1804** Query-count assertions on catalogue, detail and every admin changelist. → NFR-002, NFR-003
- [x] **T-1805** Performance check against NFR-001 and NFR-004.
- [x] **T-1806** Complete `traceability.md`: every FR → task → test, verified mechanically. → SC-014 — 122/122 claimed, 121 with test evidence, **0 problems**; two byte-identical `--check` runs (md5 `1e45217c`), both exit 0; every mapped node verified collectible by `pytest --collect-only`. **All five spec divergences resolved** — the reservation hold is released on terminal payment failure, the refund cap is enforced by a PostgreSQL constraint trigger, and the no-JS notice is accurate and bilingual. (Requirement ids deliberately not repeated here: a task line naming one *claims* it, and these belong to their own phase tasks.) Records: `qa/traceability-closure.md`, `qa/spec-divergences.md`.

---

## Phase 19 — Visual-regression verification

- [ ] **T-1901** ★ Full sweep: 13 routes × 4 viewports vs. the T-0102b baseline. *Accept*: only the two approved deltas (shipping row, installation row) differ, each with a recorded approval. → SC-008
- [x] **T-1902** Console-error and asset-404 sweep across all routes × viewports. → NFR-009
- [x] **T-1903** RTL and horizontal-overflow verification. → NFR-010
- [x] **T-1904** axe accessibility sweep; no new critical or serious violation. → NFR-007
- [x] **T-1905** No-JS suite across all 4 viewports. → FR-136
- [ ] **T-1906** `npm run qa` end-to-end green.
- [x] **T-1907** Route and slug contract re-verified (T-0103 still passing). → SC-009, SC-011

---

## Phase 20 — Production readiness and handoff

- [x] **T-2001** Deployment runbook (env vars, migrations, static, superuser, roles) → `quickstart.md` + runbook.
- [x] **T-2002** Backup and **rehearsed** restore procedure. → security §3
- [x] **T-2003** Rollback rehearsal per `rollout-and-rollback.md`. → `qa/rollback-rehearsal.md`, `tests/deployment/test_rollback_rehearsal.py` (32 tests); found and fixed a missing `cd "$ZAKEY_ROOT"` in `rollback()`.
- [x] **T-2004** Operational monitoring: health checks, error reporting, reconciliation schedule.
- [x] **T-2005** Staff documentation for the 9 roles and the core admin workflows.
- [ ] **T-2006** ⚠️ **Business-input gate**: real shipping rates and installation fee entered by the business, replacing development placeholders. → ASM-004, ASM-005
- [x] **T-2007** Final handoff report; confirm no production deployment occurred during implementation.

---

## Dependencies

```
P1 ─► P2 ─► P3 ─► P4 ─┬─► P5 ─► P6 ─┐
                      ├─► P8        ├─► P10 ─► P11 ─► P13 ─► P14 ─► P15 ─► P16 ─► P17 ─► P18 ─► P19 ─► P20
                      ├─► P9        │
                      └─► P7 ───────┘
                          P12 ──────┘ (parallel with P10–P11)
```

- **P1–P4 are blocking.** No domain work starts before ownership and auth exist.
- **P5, P7, P8, P9, P12 can run in parallel** once P4 lands (different apps, different files).
- **P10 requires P5–P9** — it locks stock and coupons.
- **P16 requires P15** — integration needs seeded data.
- **P19 requires P16**, and compares against the P1 baseline.

## Parallelisable groups

| Group | Tasks | Why safe |
|---|---|---|
| A | T-0501…T-0507 | Separate models, one app, sequential migrations |
| B | T-0801…T-0806 · T-0901…T-0904 | Different apps |
| C | T-1201…T-1206 | Independent of the order pipeline |
| D | T-1302…T-1307 | Separate admin modules |
| E | T-1701…T-1708 | Independent security concerns |

**Never parallel**: T-1004/T-1005/T-1006 (same transaction core); any two Phase-16 batches (both touch templates); anything touching migrations in the same app.

## Counts

**20 phases · 143 tasks · 99 complete · 44 open.** Claude is reviewer of record for every checked box.

| Phase | Tasks | Phase | Tasks | Phase | Tasks | Phase | Tasks |
|---|---|---|---|---|---|---|---|
| 1 Freeze | 6 | 6 Inventory | 7 | 11 Payments | 8 | 16 Integration | 9 |
| 2 Foundation | 8 | 7 Cart | 8 | 12 Reviews/Content | 6 | 17 Security | 8 |
| 3 Core | 3 | 8 Shipping | 6 | 13 Jazzmin | 10 | 18 Tests | 6 |
| 4 Accounts | 7 | 9 Promotions | 4 | 14 Permissions | 7 | 19 Visual | 7 |
| 5 Catalogue | 10 | 10 Checkout/Orders | 10 | 15 Seed | 6 | 20 Handoff | 7 |

No task exceeds ~3 primary files, per the bounded-assignment rule.
